import 'package:flutter/material.dart';

import 'home_screen.dart';

void main(){
  runApp(const MaterialApp(
    home: Scaffold(
      body: Center(
        child: HomeScreen(),
      ),
      )

  ));
}